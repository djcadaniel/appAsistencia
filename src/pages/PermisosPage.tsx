import { useDashboard } from "../context/AppContext";

export const PermisosPage = () => {

  // const { filteredData, filters, setFilter, uniqueValues, totalCantidadTarde, totalMemorandums, totalFaltas, totalPermisosPersonales, totalCompensacion } = useDashboard();
  
  return (
    <>

    </>
  )
}
